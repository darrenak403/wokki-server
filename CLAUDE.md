# Wokki Server — Claude Code

@AGENTS.md

Branch workspace rule lives in AGENTS.md: selected branch URL scopes workspace/sidebar actions; Admin creates employees with department → auto Active LocationMembership; no self-serve join. Manager only `LocationManager`-assigned locations.
